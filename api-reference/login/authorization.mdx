---
title: "Authorization"
api: "GET https://api.zebedee.io/v1/oauth2/authorize"
---

## Description

In order to properly redirect users to the ZBD Login portal, you must open a browser session containing the built `authorization URL` as the destination address.

This API endpoint helps you create that authorization URL.

## Usage

[Read here for more details](/docs/zbd-login/intro) on how to setup ZBD Login (OAuth2) with your platform.

## Configuration

### Query Parameters

<ParamField required query="client_id" type="string" />
<ParamField required query="response_type" type="string" />
<ParamField required query="redirect_uri" type="string" />
<ParamField required query="code_challenge_method" type="string" />
<ParamField required query="code_challenge" type="string" />
<ParamField required query="scope" type="string" />

<RequestExample>

```bash Request
curl -L -X GET 'https://api.zebedee.io/v1/oauth2/authorize'
```

</RequestExample>
